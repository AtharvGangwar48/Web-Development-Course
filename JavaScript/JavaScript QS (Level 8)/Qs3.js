/*let upperCase = (str) => {
    let newStr = [...str]
    console.log(newStr)
    let upCase = newStr.map((el) => {
        newStr = el.toUpperCase()
        return newStr
    })
    console.log(upCase)
}*/

letstrings = ["adam","bob","catlyn","donald","eve"]
console.log(strings.map((string)=>string.toUpperCase()))